// Reuse the exact same Scores screen used by the patient role.
import PatientDashboard from "./PatientDashboard.jsx";

export default function DoctorScores() {
  // Renders the patient Scores UI verbatim (forms, styling, behavior).
  return <PatientDashboard />;
}
